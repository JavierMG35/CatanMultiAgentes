<agent
	xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex
	                    http://jadex.sourceforge.net/jadex-0.96.xsd"
	name="Jugador"
	package="Mapa.Jugador">

	<imports>
		<import>jadex.runtime.*</import>
		<import>jadex.planlib.*</import>
		<import>jadex.util.*</import>
		<import>jadex.adapter.fipa.*</import>
		<import>java.util.logging.*</import>
		<import>java.util.*</import>
	</imports>
<!--para registrarse en el DF -->
	<capabilities>
		<capability name="dfcap" file="jadex.planlib.DF"/>
	</capabilities>
	<beliefs>
		<!-- The player's knowledge about itself, encapsulated as player object. -->
		<belief name="myself" class="Jugador" exported="true">
			<!-- Create deafult player object, when not passed as argument. -->
			<fact>
				new Jugador($scope.getAgentName(), jugador1,	AbstractStrategy.CONSTANT_VERY_CAREFUL)
			</fact>
		</belief>

		<belief name="tablero" class="Tablero" exported="true"/>

		<belief name="cartas" class="Cartas" exported="true"/>

		<belief name="puntuacion" class="int" />

			</beliefs>

<initialbelief ref = "puntuacion"
<fact>0</fact>
</initialbelief>

<!--para registrarse en el DF -->
	<goals>
		<achievegoalref name="df_register">
			<concrete ref="dfcap.df_register"/>
		</achievegoalref>
	</goals>

	<configurations>
		<configuration name="jugador">
			<goals><initialgoal ref="df_register">
				<parameter ref="description"> <value>
					SFipa.createAgentDescription(null,SFipa.createServiceDescription("jugador","jugador", null))</value></parameter>
			</initialgoal></goals>
		</configuration>

	</configurations>

</agent>
